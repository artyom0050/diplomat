// Advertising and analytics IDs. Leave "" to switch a service off.
// While both are empty, the site shows no cookie banner and loads no trackers.
window.TRACKING = {
  ga4: "G-94ZT7SGFPL",      // Google Analytics 4 Measurement ID, e.g. "G-XXXXXXXXXX"
  googleAds: "AW-18456816783",// Google Ads tag ID, e.g. "AW-123456789"
  metaPixel: ""   // Meta (Facebook/Instagram) Pixel ID, e.g. "123456789012345" — placeholder, off.
                  // Before turning it on, add Meta to privacy.html (see the comment there).
};
